import { ClearCurrencyFormattingPipe } from './clear-currency-formatting.pipe';

describe('ClearCurrencyFormattingPipe', () => {
  it('create an instance', () => {
    const pipe = new ClearCurrencyFormattingPipe();
    expect(pipe).toBeTruthy();
  });
});
